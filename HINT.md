# Hint

Merge each crew's resolved branch in here, one at a time. The seal crew's
merge should go clean; the header crew's may need a hand depending on
merge order. Once both are in, run `python3 src/part_b.py` from `src/`
and see what the seal check says.
