# Hint

Good — if you rebased and landed here clean (or resolved a config
conflict to get here), you're caught up on the seal value. Next: a
trick that never made the main journal is sitting on its own branch
somewhere — `git branch -a` will turn it up. Bring it home with a
cherry-pick.
