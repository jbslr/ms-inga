# Hint

This header placeholder was never going to last. Rebase onto
`ms-inga/root` — it'll conflict, that's expected, keep the true
value. Once that's settled, a trick you'll need is sitting on a branch
that never made the main journal — `git branch -a` will turn it up.
